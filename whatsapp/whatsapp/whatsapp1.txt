<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>

    <!-- Floating WhatsApp & Chat Box -->
    <div class="whatsapp-chat-container">
        <div class="whatsapp-chat-box" id="whatsappChat">
            <div class="chat-header">
                <div class="d-flex align-items-center">
                    <img src="../images/final logo.png" alt="LN" class="rounded-circle bg-white p-1 me-2"
                        style="width: 50px; height: 50px;">
                    <div>
                        <p class="fw-bold mb-0 text-white">LN Enterprise</p>
                        <small class="text-white-50"><i class="fas fa-circle text-success small"></i> Online</small>
                    </div>
                </div>
                <span onclick="toggleWhatsApp()" class="btn-close ms-auto"></span>
            </div>
            <div class="chat-body p-3" id="waChatBody">
                <div class="message-received">
                    <p class="mb-0">Hello! 👋<br>How can we help you today with your uniform requirements?</p>
                    <small class="text-muted">Just now</small>
                </div>
            </div>
            <div class="chat-footer p-2 border-top">
                <div class="input-group">
                    <input type="text" id="waMessage" class="form-control border-0" placeholder="Type a message..."
                        onkeypress="handleWAKey(event)">
                    <button class="btn btn-success" onclick="sendWAMessage()">
                        <i class="fas fa-paper-plane"></i>
                    </button>
                </div>
            </div>
        </div>
        <button class="whatsapp-float border-0" onclick="toggleWhatsApp()" id="waToggleBtn">
            <i class="fab fa-whatsapp"></i>
        </button>
    </div>


<script src="index.js" ></script>
    
</body>
</html>






// --- WHATSAPP CHAT FUNCTIONS ---
function toggleWhatsApp() {
    const chat = document.getElementById('whatsappChat');
    chat.classList.toggle('active');
}

function sendWAMessage() {
    const msgInput = document.getElementById('waMessage');
    const chatBody = document.getElementById('waChatBody');
    const message = msgInput.value.trim();

    if (message) {
        // 1. Add User Message
        const userMsgDiv = document.createElement('div');
        userMsgDiv.className = 'message-sent';
        userMsgDiv.innerHTML = `<p class="mb-0">${message}</p><small class="text-muted" style="font-size: 0.7rem; opacity: 0.7;">Just now</small>`;
        chatBody.appendChild(userMsgDiv);

        // 2. Clear Input & Scroll
        msgInput.value = '';
        chatBody.scrollTop = chatBody.scrollHeight;

        // 3. Simulated Business Reply
        setTimeout(() => {
            const replyDiv = document.createElement('div');
            replyDiv.className = 'message-received';
            replyDiv.innerHTML = `<p class="mb-0">Got it! We're processing your request. Please wait a moment while we connect you to a specialist.</p><small class="text-muted" style="font-size: 0.7rem; opacity: 0.7;">Online</small>`;
            chatBody.appendChild(replyDiv);
            chatBody.scrollTop = chatBody.scrollHeight;

            // Optional: Provide a link to actual WhatsApp after a delay
            setTimeout(() => {
                const finalRedir = document.createElement('div');
                finalRedir.className = 'message-received';
                finalRedir.innerHTML = `<p class="mb-0">Click here to continue on official WhatsApp:</p>
                                        <a href="https://wa.me/+919876543210?text=${encodeURIComponent(message)}" target="_blank" class="btn btn-success btn-sm mt-2 rounded-pill">Continue to WhatsApp</a>`;
                chatBody.appendChild(finalRedir);
                chatBody.scrollTop = chatBody.scrollHeight;
            }, 1500);
        }, 800);
    }
}

function handleWAKey(event) {
    if (event.key === 'Enter') {
        sendWAMessage();
    }
}



/* WhatsApp Chat Box System */
.whatsapp-chat-container {
    position: fixed;
    bottom: 30px;
    right: 30px;
    z-index: 1000;
    font-family: 'Roboto', sans-serif;
}

.whatsapp-chat-box {
    position: fixed;
    bottom: 90px;
    right: 30px;
    width: 320px;
    max-width: 320px;
    background: #fff;
    border-radius: 15px;
    box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2) !important;
    display: none;
    overflow: hidden;
    transform-origin: bottom right;
    animation: chatAppear 0.3s ease-out;
    z-index: 999;
}

@keyframes chatAppear {
    from {
        opacity: 0;
        transform: scale(0.8) translateY(20px);
    }

    to {
        opacity: 1;
        transform: scale(1) translateY(0);
    }
}

.chat-header {
    background: #075e54;
    padding: 15px;
    display: flex;
    align-items: center;
    color: #fff;
}

.chat-header img {
    object-fit: contain;
}

.chat-header .btn-close {
    font-size: 0.8rem;
    padding: 10px;
    background: none !important;
    border: none;
    color: white !important;
    opacity: 1;
    cursor: pointer;
}

.chat-header .btn-close::before {
    content: "\2715";
    /* Unicode multiplication sign (X) */
    font-size: 1.2rem;
    font-weight: bold;
}

.chat-body {
    height: 300px;
    background: #e5ddd5;
    background-image: url('https://user-images.githubusercontent.com/15075759/28719144-86dc0f70-73b1-11e7-911d-60d70fcded21.png');
    overflow-y: auto;
    display: flex;
    flex-direction: column;
    padding: 15px;
}

.message-received,
.message-sent {
    padding: 8px 12px;
    border-radius: 10px;
    max-width: 85%;
    margin-bottom: 10px;
    box-shadow: 0 1px 1px rgba(0, 0, 0, 0.1);
    font-size: 0.9rem;
}

.message-received {
    background: #fff;
    border-top-left-radius: 0;
    align-self: flex-start;
}

.message-sent {
    background: #dcf8c6;
    border-top-right-radius: 0;
    align-self: flex-end;
}

.whatsapp-float {
    background-color: #25d366;
    color: white;
    width: 60px;
    height: 60px;
    border-radius: 50px;
    text-align: center;
    font-size: 30px;
    box-shadow: none !important;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.3s ease;
}

.whatsapp-float:hover {
    transform: scale(1.1) rotate(10deg);
}

.whatsapp-chat-box.active {
    display: block;
}

/* WhatsApp Chat Box Responsive Fixes */
@media (max-width: 767px) {
    .whatsapp-chat-box {
        width: 85% !important;
        max-width: 340px !important;
        right: 7.5% !important;
        left: auto !important;
        bottom: 85px !important;
    }
    
    .whatsapp-chat-container {
        right: 5%;
    }
}

@media (max-width: 575px) {
    .whatsapp-chat-box {
        width: 90% !important;
        max-width: 320px !important;
        right: 5% !important;
        left: auto !important;
        bottom: 75px !important;
    }
    
    .whatsapp-chat-container {
        right: 5%;
    }
}

/* Stat Card Content Overflow Fix */
@media (max-width: 991px) {
    .stat-card {
        min-height: 200px;
        padding: 1.25rem;
    }
    
    .stat-number {
        font-size: 2.5rem;
    }
}

@media (max-width: 767px) {
    .stat-card {
        min-height: 180px;
        padding: 1rem;
    }
    
    .stat-number {
        font-size: 2rem;
    }
}

@media (max-width: 575px) {
    .stat-card {
        min-height: 150px;
        padding: 1rem 0.5rem;
    }
    
    .stat-number {
        font-size: 1.75rem;
    }
    
    .stat-card p, .stat-card h6 {
        font-size: 0.9rem;
    }
}









